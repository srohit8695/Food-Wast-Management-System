<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodmanagementsystem">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodManagementSystem">
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsViewLocation"
            android:label="@string/title_activity_maps_view_location" />
        <activity
            android:name=".interfaces.DummyMap"
            android:label="@string/title_activity_dummy_map" />
        <activity
            android:name=".LatLonFromGMAP"
            android:label="@string/title_activity_lat_lon_from_g_m_a_p" />
        <activity android:name=".DonorHistoryView" />
        <activity android:name=".AccepterHistoryView" />
        <activity android:name=".AcceprorAcceptedBriefView" />
        <activity android:name=".AcceptorAccepted" />
        <activity android:name=".OrderAcceptorInfo" />
        <activity android:name=".OrderBriefView" />
        <activity android:name=".OrdersAvailable" />
        <activity android:name=".DonorDonateBrief" />
        <activity android:name=".DonorDonate" />
        <activity android:name=".AcceptorHomeScreen" />
        <activity android:name=".DonorHomeScreen" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Registration" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>